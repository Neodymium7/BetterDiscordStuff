{
	"name": "betterdiscordstuff",
	"description": "Plugins and CSS Snippets for BetterDiscord.",
	"devDependencies": {
		"bundlebd": "^4.0.0",
		"typescript": "^5.0.4"
	},
	"scripts": {
		"build": "npx bundlebd --plugin",
		"dev": "npx bundlebd --dev --plugin",
		"check": "tsc --noEmit"
	}
}
